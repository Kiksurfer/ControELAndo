# tapunto-voz · requirements.txt
# Reconocimiento de voz mediante Google Speech Recognition (requiere internet).

# --- Reconocimiento de voz ---
SpeechRecognition>=3.10.0

# --- Control del sistema ---
pyautogui>=0.9.54
keyboard>=0.13.5
pyperclip>=1.8.2
pywin32>=306
pycaw>=20230407
comtypes>=1.2.0

# --- Empaquetado (solo para compilación, no se incluye en el .exe) ---
pyinstaller>=6.0.0
